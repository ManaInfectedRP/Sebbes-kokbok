apiVersion: v1
kind: Service
metadata:
  name: kokboken-api
  namespace: kokboken
  labels:
    app: kokboken-api
spec:
  selector:
    app: kokboken-api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
