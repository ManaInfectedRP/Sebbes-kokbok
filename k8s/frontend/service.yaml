apiVersion: v1
kind: Service
metadata:
  name: kokboken-frontend
  namespace: kokboken
  labels:
    app: kokboken-frontend
spec:
  selector:
    app: kokboken-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
